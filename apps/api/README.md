# @open-case-memory/api

Fastify REST API for the Open Case Memory platform.

## Status

Stub — only `/api/health` is implemented.

## Scripts

- `pnpm dev` — watch mode via tsx
- `pnpm build` — compile TypeScript to `dist/`
- `pnpm start` — run compiled server

## Environment

- `PORT` (default `4000`)
- `DATABASE_URL`
