# @open-case-memory/web

Next.js 15 UI (App Router) for Open Case Memory.

## Status

Stub — only the landing page skeleton exists.

## Scripts

- `pnpm dev` — Next.js dev server
- `pnpm build` — production build
- `pnpm start` — run built server

## Environment

- `PORT` (default `3000`)
- `NEXT_PUBLIC_API_URL` — browser-facing API URL
